<div class="container">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-contents">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <!--- div class="imgLogo"></div -->
    <a href="/" class="navbar-brand logo"><span class="letter">G</span><span class="num">0</span><span class="merge"><span class="num">:</span><span class="letter">a</span></span><span class="num">1</span><span class="letter">s.Club</span></a>
  </div>
  <!-- end navbar-header -->
  <!-- Nav links and content -->
  <div class="collapse navbar-collapse" id="navbar-contents">
    <ul class="nav navbar-nav" role="navigation">
      <% if user_signed_in? %>
          <% if can? :read, Match %>
              <li><%= link_to('Matches', Match) %></li>
              <% end %>
      <% end %>
      <% if can? :create, Guess %>
          <li><%= link_to('Stats', '/') %></li>
      <% end %>
      <% if can? :create, Guess %>
          <li><%= link_to('Discussions', '/') %></li>
      <% end %>
      <% if can? :create, Guess %>
          <li><%= link_to('News', '/') %></li>
      <% end %>
      <% if can? :read, Match %>
          <li><%= link_to('Rules', '/rules') %></li>
      <% end %>
      <% if can? :read, Match %>
          <li><%= link_to('About', '/') %></li>
      <% end %>
      <% if can? :create, Match %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Admin
          <% if @new_users and @new_users.count > 0 %><span class="badge alert-success"><%= @new_users.count %></span>
          <% end %>
          <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <li><a href="/admin">Users
            <% if @new_users and @new_users.count > 0 %>
                <span class="badge alert-success pull-right"><%= @new_users.count %></span>
            <% end %></a></li>
          <li><a href="#">Matches</a></li>
          <li><a href="#">Teams</a></li>
          <li class="divider"></li>
          <li><a href="#">Competitions</a></li>
          <li><a href="#">Stages</a></li>
          <li><a href="#">Stadiums</a></li>
          <li><a href="#">Cities</a></li>
          <li><a href="#">Countries</a></li>
          <li><a href="#">Cities</a></li>
        </ul>
      </li>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">

      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= user_signed_in? ? current_user.setting.screen_name : 'Admin Panel' %>
          <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <%= render 'devise/menu/registration_items' %>
          <%= render 'devise/menu/setting_items' %>
          <%= render 'devise/menu/login_items' %>
        </ul>
      </li>


    </ul>
  </div>
  <!-- end navbar-collapse -->
</div> <!-- end container -->