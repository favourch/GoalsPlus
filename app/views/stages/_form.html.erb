<%= form_for(@stage, html: {role: 'form', class: 'form-horizontal'}) do |f| %>
    <% if @stage.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@stage.errors.count, "error") %> prohibited this stage from being saved:</h2>

          <ul>
            <% @stage.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="frontGame" style="display: box">
      <div class="panel panel-default">
        <div class="match_body">
          <div class="form-group">
            <%= f.label 'Tournament Stage', class: 'col-sm-4 control-label' %>
            <div class="col-sm-6">
              <%= f.text_field :name, class: 'form-control' %>
            </div>
          </div>
          <div class="form-group submit">
            <%#= f.submit class: 'btn btn-success btn-large', value: 'Update' %>
          </div>

          <div class="btn btn-success btn-large submitBtn" data-controller="stages">Update</div>


        </div>
      </div>
    </div>
<% end %>
